# Fonte de verdade é o pyproject.toml. Este arquivo existe para builds simples
# de Docker e para ambientes que ainda não usam PEP 621.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
aiosqlite>=0.20
alembic>=1.14
httpx>=0.27
python-multipart>=0.0.12
structlog>=24.4
sse-starlette>=2.1
# Sempre a variante headless: `opencv-python` exige libGL, que não existe
# em servidor limpo, e a aplicação nunca abre janela.
opencv-python-headless>=4.10
numpy>=1.26,<3
